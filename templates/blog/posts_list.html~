{% extends "base.html" %}
{% load pagination_tags %}
{% block title %}Blog posts{% endblock %}
{% block body %}

<h1>
	Blog posts
</h1>
   <div id="posts">
     {% autopaginate posts 10 %}
	<ul>
       {% for post in posts %}

          <li>
					<p><a href="{{ post.get_absolute_url }}" title="{{ post.title }}">{{ post.title }}</a>
					(---<a href="{% url post post_id=post.pk %}" title="edit the post '{{ post.title }}'">edit</a></h3>---<a class="title" href="{{ post.get_absolute_url }}" title="view the post '{{ post.title }}'">view</a>---<a href="{% url post_delete post_id=post.pk %}"title="delete the post '{{ post.title }}'">delete</a>)</p>
          </li>
			{% empty %}
	  <li>
					No posts yet!
	  </li>
	{% endfor %}
       </ul>

    </div>
      {% paginate %}
			
{% endblock %}
